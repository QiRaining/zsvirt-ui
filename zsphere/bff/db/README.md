# ZSvirt BFF 数据库基线

`V5.1.0__init_database.sql` 是新环境使用的完整 `zstack_ui` schema 基线，不用于升级已有数据库。

本地默认 Mock 模式使用内存 SQLite，不需要执行该文件。只有接入真实服务时，才应由环境所有者使用受控的数据库发布流程将此基线应用到一个全新数据库，并通过未提交的环境变量提供连接信息。

不要把真实数据库地址、账号或密码写入仓库。
