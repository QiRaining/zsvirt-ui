import React from "react";

export type DialogDeletionFormGuide = {
  confirmWord?: string;
  errorMessage?: string;
  guideMessage?: React.ReactNode;
};

export type DialogDeletionPasswordFormGuide = {
  errorMessage?: string;
  guideMessage?: React.ReactNode;
};
